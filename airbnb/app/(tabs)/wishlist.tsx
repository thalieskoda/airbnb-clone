import { Tabs } from "expo-router";
import React from "react";
import { View, Text } from "react-native";
const Page = () => {
  return (
    <View>
      <Text>Wishlist</Text>
    </View>
  );
};

export default Page;
